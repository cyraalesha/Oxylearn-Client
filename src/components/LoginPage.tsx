import React from "react";
import { User } from "../entities/User";

export const LoginPage = ({ setUser }: { setUser: (User: User) => void }) => {
  return <h1>you need to login first but it's not implemented xdxd</h1>;
};
